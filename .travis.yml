language: d

sudo: required
dist: bionic

branches:
  except:
    - /appveyor-.*/
    - /circleci-.*/
    - gh-pages

d:
  - dmd
  - ldc

os:
  - linux
  - osx

script:
  - dub test  --compiler=${DC}
  - dub build --compiler=${DC} -b debug
  - dub build --compiler=${DC} -b debug -c twitch
  - dub build --compiler=${DC} -b debug -c dev
  - dub build --compiler=${DC} -b plain
  - dub build --compiler=${DC} -b plain -c twitch
  - dub build --compiler=${DC} -b plain -c dev
  - dub build --compiler=${DC} -b release
  - dub build --compiler=${DC} -b release -c twitch
  - dub build --compiler=${DC} -b release -c dev
  - dub build --compiler=${DC} -b debug :defs
  - dub build --compiler=${DC} -b debug :defs -c twitch
  - dub build --compiler=${DC} -b debug :assertgen
  - dub build --compiler=${DC} -b plain :assertgen
  - dub build --compiler=${DC} -b release :assertgen

before_deploy:
  - dub build --compiler=${DC} -b ddox
  - cp -rv .circleci docs/

deploy:
  provider: pages
  local_dir: docs
  skip_cleanup: true
  github_token: $GH_REPO_TOKEN
  on:
    branch: master
    condition:
      - ${DC} = dmd
      - ${TRAVIS_OS_NAME} = linux
  cache:
    directories:
      - $HOME/.dub
  verbose: true
